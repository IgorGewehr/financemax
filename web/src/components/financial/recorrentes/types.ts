/**
 * View-model da tela Financeiro › Recorrentes (SDD). Ver `docs/ui/financeiro-ui.md`.
 * Dinheiro é SEMPRE `Centavos` (inteiro) — nunca float de reais (`lib/money`).
 */

export type LenteRecorrentes = 'fixas' | 'assinaturas';
